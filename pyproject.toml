[project]
name = "jarvis"
version = "0.1.0"
description = "Chatbot Agentique avec FastAPI et Ollama"
authors = [
    {name = "titouan", email = "titouanmok@outlook.com"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.11,<4.0.0"
dependencies = [
    "fastapi[standard] (>=0.128.0,<0.129.0)",
    "uvicorn (>=0.32.0)", 
    "langchain (>=0.3.0)",        
    "langchain-ollama (>=0.2.0)", 
    "pydantic-settings (>=2.6.0)", 
    "langchain-core (>=1.2.7,<2.0.0)", 
    "pytest (>=9.0.2,<10.0.0)", 
    "langchain-mistralai (>=1.1.1,<2.0.0)", 
    "streamlit (>=1.52.2,<2.0.0)", 
    "sqlalchemy (>=2.0.47,<3.0.0)", 
    "alembic (>=1.18.4,<2.0.0)", 
    "psycopg[binary] (>=3.3.3,<4.0.0)",
    "python-jose[cryptography] (>=3.3.0,<4.0.0)",
    "passlib[bcrypt] (>=1.7.4,<2.0.0)",
    "bcrypt (>=4.0.1,<5.0.0)"
]

[tool.poetry]
package-mode = false  # <--- INDISPENSABLE pour votre architecture

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
